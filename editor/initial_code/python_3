def eh_escher(n, alturas):
    # escreva aqui seu código
    return None


print("Example:")
print(eh_escher(3,[2,1,0]))

# As funções assert são utilizadas para verificar se o resultado da função chamada é o esperado
# Teste 1: Perfil Escher com sequência par
assert eh_escher(4, [1, 2, 1, 2]) == 'S'

# Teste 2: Perfil Escher com sequência ímpar
assert eh_escher(5, [1, 2, 3, 1, 2]) == 'S'

# Teste 3: Perfil não Escher com sequência par
assert eh_escher(4, [1, 2, 2, 1]) == 'N'

# Teste 4: Perfil não Escher com sequência ímpar
assert eh_escher(5, [1, 2, 3, 2, 1]) == 'N'
    
print("Seu código passou pelos testes, clique em Check para avançar!")
